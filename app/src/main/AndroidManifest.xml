<?xml version="1.0" encoding="utf-8"?>
<!-- Author Aarni Pesonen, Samu Wahlroos, Johannes Jokinen -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.team1.hyteproject">

    <uses-permission android:name="android.permission.READ_CALENDAR" />
    <uses-permission android:name="android.permission.WRITE_CALENDAR" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Apptitudo"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Default">
        <activity android:name="com.team1.hyteproject.ui.RegistrationActivity"></activity>
        <activity android:name="com.team1.hyteproject.HomeActivity" />
        <activity
            android:name="com.team1.hyteproject.ui.LoginActivity"
            android:label="Apptitudo">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>